"use client";

import { FormEvent, useEffect, useMemo, useState } from "react";
import { categoryForRegistration } from "@/lib/category-rules";

type EventData = {
  event: { slug:string; name:string; description:string|null; location:string|null; starts_on:string; ends_on:string; registration_source:string; participant_limit:number|null; windfit_registration_url:string|null; terms_url:string|null; access_mode:string; registration_open:boolean };
  stages: Array<{id:string;stage_number:number;name:string;route_label:string|null;stage_date:string;distance_km:number|null;elevation_m:number|null}>;
  availability: {registered:number;remaining:number|null;available:boolean;closed_by_date:boolean;full:boolean};
};
type Success = { registration_code:string; full_name:string; email:string; category:string; modality:string };

const fieldStyle = { width:"100%",boxSizing:"border-box" as const,padding:"14px",border:"1px solid #b9ad9c",background:"#fffaf2",color:"#171917",font:"inherit" };

export default function RegistrationClient({slug}:{slug:string}) {
  const [data,setData]=useState<EventData|null>(null); const [loading,setLoading]=useState(true); const [saving,setSaving]=useState(false);
  const [error,setError]=useState(""); const [success,setSuccess]=useState<Success|null>(null);
  const [form,setForm]=useState({full_name:"",email:"",phone:"",birth_date:"",gender:"",modality:"gravel_race",city:"",state:"",country:"Brasil",terms_accepted:false,privacy_accepted:false,website:""});
  const categoryPreview=useMemo(()=>data&&form.birth_date&&form.gender?categoryForRegistration({birthDate:form.birth_date,eventDate:data.event.starts_on,gender:form.gender,modality:form.modality}):null,[data,form.birth_date,form.gender,form.modality]);

  useEffect(()=>{fetch(`/api/events/${encodeURIComponent(slug)}`,{cache:"no-store"}).then(async response=>{const payload=await response.json();if(!response.ok)throw new Error(payload.error??"Evento não encontrado.");setData(payload);}).catch(err=>setError(err.message)).finally(()=>setLoading(false));},[slug]);
  function update(field:string,value:string|boolean){setForm(current=>({...current,[field]:value}));}
  async function submit(event:FormEvent){event.preventDefault();setSaving(true);setError("");try{const response=await fetch(`/api/events/${encodeURIComponent(slug)}/register`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const payload=await response.json();if(!response.ok){if(payload.redirect_url)window.location.href=payload.redirect_url;throw new Error(payload.error??"Não foi possível concluir a inscrição.");}setSuccess(payload.registration);window.localStorage.setItem("legends-pending-registration",JSON.stringify({code:payload.registration.registration_code,email:payload.registration.email,event:slug}));setData(current=>current?{...current,availability:{...current.availability,registered:current.availability.registered+1,remaining:current.availability.remaining===null?null:Math.max(0,current.availability.remaining-1)}}:current);}catch(err){setError(err instanceof Error?err.message:"Não foi possível concluir a inscrição.");}finally{setSaving(false);}}
  function copyCode(){if(success)navigator.clipboard.writeText(success.registration_code).catch(()=>{});}

  if(loading)return <main className="public-event loading">Carregando evento...</main>;
  if(!data)return <main className="public-event loading"><div><h1>Evento indisponível</h1><p>{error}</p><a href="/">Voltar ao site</a></div></main>;
  const {event,stages,availability}=data; const windfitOnly=event.registration_source==="windfit";
  return <main className="public-event"><style>{`
    .public-event{min-height:100vh;background:#0d100d;color:#f3eee5;font-family:Arial,sans-serif}.public-event *{box-sizing:border-box}.loading{display:grid;place-items:center;padding:30px}.event-top{min-height:420px;background:linear-gradient(90deg,#070907f2,#070907a8),url('/hero-production.jpg') center/cover;padding:30px 5vw 58px}.event-nav{display:flex;align-items:center;justify-content:space-between}.event-nav img{width:170px}.event-nav a{color:#ddd;text-decoration:none;font-size:12px;font-weight:800;text-transform:uppercase}.event-hero{width:min(1400px,100%);margin:76px auto 0}.event-kicker{color:#e0792e;font-size:12px;letter-spacing:.22em;font-weight:900;text-transform:uppercase}.event-hero h1{font-size:clamp(52px,7vw,104px);line-height:.88;font-weight:300;margin:16px 0 24px}.event-hero p{font-size:19px;line-height:1.65;color:#bdc0b9;max-width:760px}.event-facts{display:flex;gap:28px;flex-wrap:wrap;margin-top:28px}.event-facts div{border-left:1px solid #b95e23;padding-left:14px}.event-facts strong,.event-facts span{display:block}.event-facts strong{font-size:23px}.event-facts span{color:#999;font-size:11px;text-transform:uppercase;margin-top:4px}.registration-layout{width:min(1400px,90%);margin:0 auto;padding:70px 0 100px;display:grid;grid-template-columns:.75fr 1.25fr;gap:36px;align-items:start}.stage-panel{border:1px solid #383d36;background:#151814;padding:28px}.stage-panel h2,.form-panel h2{font-size:34px;font-weight:400;margin:8px 0 20px}.stage-row{padding:17px 0;border-top:1px solid #383d36}.stage-row strong,.stage-row span{display:block}.stage-row span{color:#999;font-size:13px;margin-top:6px}.availability{margin-top:24px;border:1px solid #70451f;background:#25190f;padding:18px;color:#efb078}.form-panel{background:#eee5d8;color:#171917;padding:34px}.form-panel>p{color:#60655e;line-height:1.6}.form-grid{display:grid;grid-template-columns:1fr 1fr;gap:15px;margin-top:24px}.form-grid label{display:grid;gap:7px;font-size:12px;font-weight:800}.wide{grid-column:1/-1}.checks{grid-column:1/-1;display:grid;gap:10px;border-top:1px solid #c8bcac;padding-top:17px}.checks label{display:flex;grid-template-columns:auto 1fr;align-items:start;font-weight:400;line-height:1.45}.checks input{margin-top:3px}.submit{grid-column:1/-1;padding:18px;border:0;background:#e86619;color:white;font-size:16px;font-weight:900;cursor:pointer}.submit:disabled{opacity:.6}.form-error{grid-column:1/-1;background:#f3dcd6;border:1px solid #b65b46;color:#7c2d21;padding:14px}.closed{padding:28px;border:1px solid #8b5427;background:#281b11;color:#efb078}.windfit-button,.passport-button{display:block;padding:18px;background:#e86619;color:#fff;text-align:center;text-decoration:none;font-weight:900;margin-top:22px}.success{border:1px solid #31734d;background:#edf5ef;padding:26px}.success h2{color:#255e3c}.access-code{border:1px dashed #c36118;background:white;padding:18px;text-align:center;margin:20px 0}.access-code strong{display:block;font:900 30px monospace;color:#c36118}.access-code button{margin-top:9px;border:0;background:transparent;color:#555;text-decoration:underline;cursor:pointer}.success-steps{color:#555;line-height:1.7}.website{position:absolute;left:-9999px}@media(max-width:850px){.registration-layout{grid-template-columns:1fr;width:min(94%,700px);padding:45px 0 70px}.event-top{padding-inline:22px}.event-nav img{width:140px}.event-hero{margin-top:55px}.form-grid{grid-template-columns:1fr}.wide,.checks,.submit,.form-error{grid-column:auto}.form-panel{padding:24px}.event-facts{gap:16px}}
  `}</style>
    <section className="event-top"><nav className="event-nav"><a href="/"><img src="/legends-logo-official.png" alt="Legends"/></a><div style={{display:"flex",gap:20,alignItems:"center"}}><a href={`/resultados/${event.slug}`}>Resultados ao vivo</a><a href="/passport/acesso">Já sou inscrito →</a></div></nav><div className="event-hero"><div className="event-kicker">INSCRIÇÃO · LEGENDS PASSPORT</div><h1>{event.name}</h1><p>{event.description||"Uma experiência criada para pedalar, descobrir novos percursos e compartilhar a aventura."}</p><div className="event-facts"><div><strong>{event.location||"A definir"}</strong><span>Local</span></div><div><strong>{new Date(`${event.starts_on}T12:00:00`).toLocaleDateString("pt-BR")}</strong><span>Início</span></div><div><strong>{stages.length}</strong><span>Etapas</span></div><div><strong>{availability.remaining??"Sem limite"}</strong><span>Vagas disponíveis</span></div></div></div></section>
    <section className="registration-layout"><aside className="stage-panel"><div className="event-kicker">PROGRAMAÇÃO</div><h2>Etapas do evento</h2>{stages.map(stage=><div className="stage-row" key={stage.id}><strong>Dia {stage.stage_number} · {stage.name}</strong><span>{new Date(`${stage.stage_date}T12:00:00`).toLocaleDateString("pt-BR")} · {stage.route_label||"Percurso a definir"}</span>{stage.distance_km?<span>{stage.distance_km} km · {stage.elevation_m??"—"} m+</span>:null}</div>)}<div className="availability"><strong>{availability.registered} inscritos</strong><br/>{availability.remaining===null?"Evento sem limite definido.":`${availability.remaining} vagas ainda disponíveis.`}</div></aside>
      <article className="form-panel"><div className="event-kicker">GARANTA SUA PARTICIPAÇÃO</div>{success?<div className="success"><h2>Inscrição confirmada!</h2><p>{success.full_name}, seus dados já aparecem na lista oficial do evento.</p><div className="access-code"><span>Seu código de acesso</span><strong>{success.registration_code}</strong><button onClick={copyCode}>Copiar código</button></div><p className="success-steps">Guarde este código. Agora conecte sua conta Ride with GPS e use o código para liberar o Passport.</p><a className="passport-button" href="/passport/acesso">Conectar Ride with GPS →</a></div>:windfitOnly?<><h2>Inscrição pela Windfit</h2><p>Este evento utiliza a Windfit para inscrição e pagamento. Depois da confirmação, seus dados serão sincronizados automaticamente com o Legends Passport.</p>{event.windfit_registration_url?<a className="windfit-button" href={event.windfit_registration_url}>INSCREVA-SE PELA WINDFIT →</a>:<div className="closed">O link da Windfit ainda não foi configurado.</div>}</>:!availability.available?<><h2>Inscrições indisponíveis</h2><div className="closed">{availability.full?"As vagas estão esgotadas.":availability.closed_by_date?"O período de inscrições foi encerrado.":"As inscrições ainda não estão abertas ao público."}</div></>:<><h2>Inscreva-se</h2><p>Preencha seus dados. Sua categoria será definida automaticamente pelo ano-base do evento.</p><form className="form-grid" onSubmit={submit}><label className="wide">Nome completo<input style={fieldStyle} required value={form.full_name} onChange={e=>update("full_name",e.target.value)}/></label><label>E-mail<input style={fieldStyle} required type="email" value={form.email} onChange={e=>update("email",e.target.value)}/></label><label>WhatsApp<input style={fieldStyle} required type="tel" value={form.phone} onChange={e=>update("phone",e.target.value)}/></label><label>Data de nascimento<input style={fieldStyle} required type="date" value={form.birth_date} onChange={e=>update("birth_date",e.target.value)}/></label><label>Gênero<select style={fieldStyle} required value={form.gender} onChange={e=>update("gender",e.target.value)}><option value="">Selecione</option><option value="male">Masculino</option><option value="female">Feminino</option><option value="other">Outro/Prefiro não informar</option></select></label><label>Modalidade<select style={fieldStyle} value={form.modality} onChange={e=>update("modality",e.target.value)}><option value="gravel_race">Competitiva</option><option value="experience">Experience</option></select></label>{categoryPreview?<div className="wide" style={{padding:14,border:"1px solid #c8bcac",background:categoryPreview.error?"#f3dcd6":"#fffaf2"}}><strong>Categoria automática: {categoryPreview.category??"Não elegível"}</strong><br/><span style={{fontSize:12,color:"#666"}}>{categoryPreview.error??`Idade-base: ${categoryPreview.age} anos em ${event.starts_on.slice(0,4)}`}</span></div>:null}<label>Cidade<input style={fieldStyle} required value={form.city} onChange={e=>update("city",e.target.value)}/></label><label>Estado<input style={fieldStyle} value={form.state} onChange={e=>update("state",e.target.value)}/></label><label>País<input style={fieldStyle} value={form.country} onChange={e=>update("country",e.target.value)}/></label><label className="website">Não preencher<input tabIndex={-1} value={form.website} onChange={e=>update("website",e.target.value)}/></label><div className="checks"><label><input type="checkbox" checked={form.terms_accepted} onChange={e=>update("terms_accepted",e.target.checked)}/><span>Li e aceito o {event.terms_url?<a href={event.terms_url} target="_blank">regulamento e termo de responsabilidade</a>:"regulamento e termo de responsabilidade do evento"}.</span></label><label><input type="checkbox" checked={form.privacy_accepted} onChange={e=>update("privacy_accepted",e.target.checked)}/><span>Autorizo o tratamento dos meus dados para inscrição, comunicação e operação do evento.</span></label></div>{error&&<div className="form-error">{error}</div>}<button className="submit" disabled={saving||!form.terms_accepted||!form.privacy_accepted||Boolean(categoryPreview?.error)}>{saving?"CONFIRMANDO...":"CONFIRMAR INSCRIÇÃO"}</button></form></>}</article>
    </section>
  </main>;
}
